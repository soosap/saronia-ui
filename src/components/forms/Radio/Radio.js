/* @flow */
import React from 'react';
import styled from 'styled-components';

type Props = {

};

const Radio = styled.button`
  display: flex;
`;

export default (props: Props) => <Radio {...props} />;
