/* @flow */
import React from 'react';
import styled from 'styled-components';

type Props = {};

const Wrapper = styled.div``;

const InputWithIcons = (props: Props) => {
  return (
    <Wrapper>
      I am a InputWithIcons
    </Wrapper>
  );
};

export default InputWithIcons;
